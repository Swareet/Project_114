function preload(){
}

function setup(){
    canvas = createCanvas(400,400);
    canvas.position(470);
    video = createCapture(VIDEO);
    video.hide();
}

function draw(){ 
}

function snapshot()
{
}
